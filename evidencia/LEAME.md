Aquí van las salidas del perfilado:

- `callgrind.txt`: salida de `callgrind_annotate` sobre el programa lento.
- `perf.txt`: salida de `perf stat` sobre el programa lento.

Las dos se generan con `make perfil` y `make perfstat`.
